# ---- Core UI ----
streamlit==1.40.2

# ---- RAG framework ----
langchain==0.3.13
langchain-core==0.3.34
langchain-text-splitters==0.3.4
langchain-openai==0.2.14
langchain-anthropic==0.3.3
langchain-community==0.3.13

# ---- Vector store ----
# 0.5.4 pairs with chroma-hnswlib 0.7.5, which ships prebuilt wheels for
# CPython 3.12 / Windows (newer pins force a source build needing MSVC).
chromadb==0.5.4
chroma-hnswlib==0.7.5

# ---- LLM / Embedding providers ----
openai==1.58.1
anthropic==0.42.0

# ---- File parsing ----
pymupdf==1.25.1
python-docx==1.1.2

# ---- Environment / config ----
python-dotenv==1.0.1

# ---- User accounts (Supabase Postgres) ----
supabase==2.30.1

# ---- Bilingual (English + Hindi) support ----
langdetect==1.0.9
deep-translator==1.11.4

# ---- Tokenisation (token-aware chunking) ----
tiktoken==0.8.0
